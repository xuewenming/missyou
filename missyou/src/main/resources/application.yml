spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none
  jackson:
    property-naming-strategy: SNAKE_CASE
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: true

#自定义api路由配置
app:
  api-package: com.app.missyou.api


#测试配置
test:
  sample:
    mysql:
      url: 127.0.0.1
      port: 99999
  condition: dinana

